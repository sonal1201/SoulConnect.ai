import { tryCatch } from "../middlewares/tryCatch.middleware"

import { Request, Response } from "express"

export const profileSummary = tryCatch(
    async (req: Request, res: Response) => {

    }
)

export const AiprofileSearch = tryCatch(
    async (req: Request, res: Response) => {

    }
)